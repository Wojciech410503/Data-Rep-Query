let z = (num1, num2) => {
    // Arrow function that takes two parameters and logs their sum to the console
    console.log(num1 + num2);
}

z(2, 1); // Call the arrow function with arguments 2 and 1
